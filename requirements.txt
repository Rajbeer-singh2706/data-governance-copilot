# ═══════════════════════════════════════════════════════════════════════════
# Data Governance Copilot — requirements.txt
#
# IMPORTANT: All langchain-* packages must stay on the 0.3.x line together.
#   langchain-groq 1.x requires langchain-core>=1.x (a different major line)
#   langchain-groq 0.3.8 is the latest that works with langchain-core 0.3.76
#
# Resolved with: uv pip install ... --dry-run  (Python 3.12)
# ═══════════════════════════════════════════════════════════════════════════

# ── Core ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0,<3.0.0

# ── LangChain 0.3.x ecosystem (pinned together — do NOT mix 0.3.x / 1.x) ──
langchain==0.3.27
langchain-core==0.3.76
langchain-community==0.3.29
langchain-openai==0.3.33
langsmith>=0.2.0

# ── LangGraph + memory ────────────────────────────────────────────────────
langgraph>=1.0.0,<2.0.0
langgraph-checkpoint-sqlite>=3.0.0

# ── LLM providers ─────────────────────────────────────────────────────────
# groq: 0.3.8 is the latest version compatible with langchain-core 0.3.x
langchain-groq==0.3.8

# litellm: 0.6.x series is compatible with langchain-core 0.3.x
langchain-litellm>=0.3.0,<0.7.0

# ── Caching ───────────────────────────────────────────────────────────────
redis>=5.0.0
hiredis>=2.3.0

# ── UI ────────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── API (Day 16+) — uncomment when needed ─────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
slowapi>=0.1.9
sse-starlette>=2.1.0
httpx>=0.27.0         # for TestClient in test
python-multipart>=0.0.9


# ── pgvector (Day 18+) — uncomment when needed ────────────────────────────
# pgvector>=0.3.0
# psycopg2-binary>=2.9.9
# sqlalchemy>=2.0.0
# langchain-postgres>=0.0.9