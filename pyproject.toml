[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "data-governance-copilot"
version = "0.1.0"
description = "Multi-agent Data Governance Copilot"


[tool.setuptools.packages.find]
where = ["src"]